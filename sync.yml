trigger:
  branches:
    include:
    - main

resources:
  repositories:
    - repository: templates
      type: git
      name: Plumbing/core-templates
    - repository: API---deprecated
      type: git
      name: Open/API---deprecated

stages:
- template: stages/sync/azure-sync.yml@templates  # Template reference
  parameters:
    checkout: API---deprecated
